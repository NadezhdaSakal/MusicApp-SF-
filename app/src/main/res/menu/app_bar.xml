<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_baseline_search"
        android:title="@string/search"
        app:showAsAction="always"  />

    <item
        android:id="@+id/favorites"
        android:icon="@drawable/ic_baseline_favorite"
        android:title="@string/favorite"
        app:showAsAction="always"/>
    <item
        android:id="@+id/_latest"
        android:icon="@drawable/ic_baseline_latest"
        android:title="@string/latest"
        app:showAsAction="always"/>
    <item
        android:id="@+id/playlist"
        android:icon="@drawable/ic_baseline_playlists"
        android:title="@string/playlist"
        app:showAsAction="always"/>
</menu>
